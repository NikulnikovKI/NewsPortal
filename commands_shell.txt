1. Создать двух пользователей (с помощью метода User.objects.create_user('username')).
user1 = User.objects.create_user(username='Иван', first_name='Иванов')
user2 = User.objects.create_user(username='Петр', first_name='Петров')

2. Создать два объекта модели Author, связанные с пользователями.
author = Author.objects.create(authorUser=user1)
author = Author.objects.create(authorUser=user2)

3. Добавить 4 категории в модель Category.
Category.objects.create(name='Технологии')
Category.objects.create(name='Новости')
Category.objects.create(name='Образование')
Category.objects.create(name='Развлечения')
4. Добавить 2 статьи и 1 новость.
news1 = Post.objects.create(author=author1,categoryType=Post.NEWS,title='Новости о технологиях',text='Краткое содержание новости')
article1 = Post.objects.create(author=author2,categoryType=Post.ARTICLE,title='Учебная статья',text='Длинный текст')
article2 = Post.objects.create(author=author1,categoryType=Post.ARTICLE,title='История',text='Обзор')
5. Присвоить им категории (как минимум в одной статье/новости должно быть не меньше 2 категорий).
cat_news = Category.objects.get(name='Новости')
cat_entertainment = Category.objects.get(name='Развлечения')
PostCategory.objects.create(postTrough=news1, categoryTrough=cat_news)
PostCategory.objects.create(postTrough=news1, categoryTrough=cat_entertainment)
cat_education = Category.objects.get(name='Образование')
PostCategory.objects.create(postTrough=article1, categoryTrough=cat_education)
cat_tech = Category.objects.get(name='Технологии')
PostCategory.objects.create(postTrough=article2, categoryTrough=cat_tech
6. Создать как минимум 4 комментария к разным объектам модели Post (в каждом объекте должен быть как минимум один комментарий).
Comment.objects.create(commentPost=news1, commentUser=user2, text='Очень интересно!')
Comment.objects.create(commentPost=article1, commentUser=user1, text='Отличная статья!')
Comment.objects.create(commentPost=article2, commentUser=user2, text='Заслуживает внимания.')
Comment.objects.create(commentPost=article2, commentUser=user1, text='Понравилось содержание.')
7. Применяя функции like() и dislike() к статьям/новостям и комментариям, скорректировать рейтинги этих объектов.
news1.like()
article1.like()
article1.dislike()
comments = Comment.objects.all()
comments[0].like()
comments[1].dislike()
8. Обновить рейтинги пользователей.
author1.update_rating()
author2.update_rating()
9. Вывести username и рейтинг лучшего пользователя (применяя сортировку и возвращая поля первого объекта).
top_author = Author.objects.all().order_by('-ratingAuthor').first()
print(f'Лучший пользователь: {top_author.authorUser.username}, рейтинг: {top_author.ratingAuthor}')
10. Вывести дату добавления, username автора, рейтинг, заголовок и превью лучшей статьи, основываясь на лайках/дислайках к этой статье.
top_post = Post.objects.all().order_by('-rating').first()
print(f'Дата: {top_post.dateCreation}')
print(f'Автор: {top_post.author.authorUser.username}')
print(f'Рейтинг: {top_post.rating}')
print(f'Заголовок: {top_post.title}')
print(f'Превью: {top_post.preview()}')
11.Вывести все комментарии (дата, пользователь, рейтинг, текст) к этой статье.
comments_for_post = Comment.objects.filter(commentPost=top_post)
    for c in comments_for_post:
        print(f'Дата: {c.dateCreation}, Пользователь: {c.commentUser.username}, Рейтинг: {c.rating}, Текст: {c.text}')